apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: default
spec:
  selector:
    name: redis
  ports:
    - name: redis-service-port
      protocol: TCP
      port: 6379
      targetPort: redis-port
